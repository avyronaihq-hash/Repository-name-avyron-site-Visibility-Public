import React, { useRef } from 'react';
import { Helmet } from 'react-helmet';
import { Link } from 'react-router-dom';
import { motion, useScroll, useTransform } from 'framer-motion';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { Card, CardContent } from '@/components/ui/card';
import { 
  ArrowRight, 
  Shield, 
  Zap, 
  Brain, 
  TrendingUp, 
  Users, 
  Lock,
  AlertTriangle,
  DollarSign,
  Package,
  UserCheck,
  MessageSquare,
  Clock,
  CheckCircle2,
  Sparkles,
  Target,
  Rocket
} from 'lucide-react';

const HomePage = () => {
  const heroRef = useRef(null);
  const { scrollYProgress } = useScroll({
    target: heroRef,
    offset: ["start start", "end start"]
  });

  const heroOpacity = useTransform(scrollYProgress, [0, 1], [1, 0]);
  const heroScale = useTransform(scrollYProgress, [0, 1], [1, 0.8]);

  const featureBadges = [
    { icon: Shield, text: 'Not a CRM. A Control System' },
    { icon: Zap, text: 'Real-Time Risk Detection' },
    { icon: Brain, text: 'AI-Powered Intelligence' },
    { icon: TrendingUp, text: 'Payment Intelligence' },
    { icon: Users, text: 'Team Load Balancing' },
    { icon: Lock, text: 'Vendor Memory System' },
    { icon: Clock, text: '24/7 AI Agents' },
  ];

  const corePillars = [
    {
      icon: CheckCircle2,
      title: 'Lifecycle Enforcement',
      description: 'Every booking follows a structured path from inquiry to closure. No chaos, no missed steps.',
      color: 'from-indigo-500 to-blue-500'
    },
    {
      icon: Zap,
      title: 'Automation Engine',
      description: 'Automatic reminders, deadline tracking, and workflow triggers that keep operations moving.',
      color: 'from-violet-500 to-purple-500'
    },
    {
      icon: DollarSign,
      title: 'Payment Intelligence',
      description: 'Visual payment timelines, overdue alerts, and cash flow visibility at a glance.',
      color: 'from-emerald-500 to-green-500'
    },
    {
      icon: Package,
      title: 'Vendor Memory',
      description: 'Track vendor reliability, response times, and performance across all bookings.',
      color: 'from-blue-500 to-cyan-500'
    },
    {
      icon: Users,
      title: 'Team Load Intelligence',
      description: 'See who\'s overloaded, who\'s available, and distribute work intelligently.',
      color: 'from-orange-500 to-red-500'
    },
    {
      icon: MessageSquare,
      title: 'Founder Control Feed',
      description: 'Daily WhatsApp briefings with risks, overdue items, and critical actions.',
      color: 'from-pink-500 to-rose-500'
    },
  ];

  const briefingItems = [
    { icon: AlertTriangle, text: '3 bookings at risk', color: 'text-red-500', bg: 'bg-red-50' },
    { icon: DollarSign, text: '₹2.4L overdue', color: 'text-orange-500', bg: 'bg-orange-50' },
    { icon: Package, text: '4 confirmations pending', color: 'text-yellow-500', bg: 'bg-yellow-50' },
    { icon: UserCheck, text: 'Priya overloaded (12 bookings)', color: 'text-blue-500', bg: 'bg-blue-50' },
  ];

  const evolutionStages = [
    {
      stage: 'See',
      title: 'Complete Visibility',
      description: 'Know exactly what\'s happening across all bookings, payments, and vendors.',
      features: ['Real-time dashboard', 'Payment timelines', 'Team workload view']
    },
    {
      stage: 'Control',
      title: 'Operational Command',
      description: 'Enforce processes, automate workflows, and prevent issues before they happen.',
      features: ['Lifecycle enforcement', 'Automated reminders', 'Risk detection']
    },
    {
      stage: 'Intelligence',
      title: 'Predictive Power',
      description: 'AI learns your business patterns and provides proactive recommendations.',
      features: ['AI agents', 'Predictive alerts', 'Smart automation']
    },
  ];

  return (
    <>
      <Helmet>
        <title>Avyron - Operational Control Infrastructure For High-Growth Tour Operators</title>
        <meta name="description" content="Transform chaos into clarity with AI-powered command systems. Unify bookings, payments, and vendors in one intelligent control platform." />
      </Helmet>

      <div className="min-h-screen bg-gray-50 pt-24 pb-12">
        {/* Stripe-like Hero Section */}
        <motion.section 
          ref={heroRef}
          style={{ opacity: heroOpacity, scale: heroScale }}
          className="relative w-[95%] mx-auto min-h-[85vh] rounded-[2.5rem] flex items-center justify-center overflow-hidden shadow-2xl"
        >
          {/* Gradient Background */}
          <div className="absolute inset-0 bg-gradient-to-br from-indigo-900 via-violet-800 to-blue-900 z-0"></div>
          
          {/* Subtle Grid Overlay */}
          <div className="absolute inset-0 grid-bg-dense opacity-20 z-0"></div>
          
          {/* Dark Overlay for Readability */}
          <div className="absolute inset-0 bg-black/20 z-0"></div>

          {/* Hero Content */}
          <div className="relative z-10 container mx-auto px-4 sm:px-6 lg:px-8 text-center flex flex-col items-center">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, delay: 0.1 }}
              className="max-w-4xl mx-auto"
            >
              <Badge className="mb-6 bg-white/10 text-white border-white/20 backdrop-blur-md px-4 py-2 shadow-lg">
                <Sparkles className="w-4 h-4 mr-2" />
                Operational Control Infrastructure
              </Badge>
              
              <h1 className="text-5xl md:text-7xl font-extrabold text-white mb-6 leading-tight tracking-tight drop-shadow-md">
                Operational Control Infrastructure<br />
                <span className="text-transparent bg-clip-text bg-gradient-to-r from-emerald-300 to-cyan-300">For High-Growth Tour Operators</span>
              </h1>
              
              <p className="text-xl md:text-2xl text-indigo-100 mb-12 max-w-3xl mx-auto leading-relaxed font-medium drop-shadow">
                Unify bookings, payments, and vendors in one AI-powered command system. 
                Stop firefighting. Start controlling.
              </p>

              <div className="flex flex-col sm:flex-row gap-4 justify-center items-center">
                <Link to="/book-demo">
                  <Button size="lg" className="bg-gradient-to-r from-indigo-600 via-violet-600 to-blue-500 hover:from-indigo-700 hover:via-violet-700 hover:to-blue-600 text-white px-8 py-6 text-lg font-bold shadow-xl hover:scale-105 transition-transform rounded-xl border-0">
                    Book A Demo
                    <ArrowRight className="ml-2 w-5 h-5" />
                  </Button>
                </Link>
                <Link to="/product">
                  <Button size="lg" className="bg-gradient-to-r from-indigo-600 via-violet-600 to-blue-500 hover:from-indigo-700 hover:via-violet-700 hover:to-blue-600 text-white px-8 py-6 text-lg font-semibold shadow-xl hover:scale-105 transition-transform rounded-xl border-0">
                    View Platform Architecture
                  </Button>
                </Link>
              </div>
            </motion.div>
          </div>

          {/* Scroll Indicator */}
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 1.5, duration: 1 }}
            className="absolute bottom-8 left-1/2 transform -translate-x-1/2 z-10"
          >
            <motion.div
              animate={{ y: [0, 10, 0] }}
              transition={{ repeat: Infinity, duration: 2 }}
              className="w-6 h-10 border-2 border-white/30 rounded-full flex items-start justify-center p-2 backdrop-blur-sm"
            >
              <motion.div className="w-1.5 h-1.5 bg-white rounded-full" />
            </motion.div>
          </motion.div>
        </motion.section>

        {/* Proof Strip */}
        <section className="py-16 bg-transparent">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="text-center mb-12"
            >
              <h2 className="text-3xl font-bold text-gray-900 mb-4">
                Not a CRM. A Control System.
              </h2>
            </motion.div>

            <div className="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-7 gap-4">
              {featureBadges.map((badge, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, scale: 0.8 }}
                  whileInView={{ opacity: 1, scale: 1 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.1 }}
                  whileHover={{ scale: 1.05 }}
                  className="bg-white shadow-sm border border-gray-100 p-4 rounded-xl text-center hover-lift"
                >
                  <badge.icon className="w-8 h-8 mx-auto mb-2 text-indigo-600" />
                  <p className="text-xs font-medium text-gray-700">{badge.text}</p>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* Growth Without Systems Breaks */}
        <section className="py-24 bg-white rounded-[3rem] mx-4 shadow-sm border border-gray-100">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Growth Without Systems <span className="text-gradient-indigo">Breaks</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                At 20 bookings/month, Excel works. At 50, you're stressed. At 100, you're drowning. 
                Avyron is the infrastructure that scales with you.
              </p>
            </motion.div>

            <div className="grid md:grid-cols-3 gap-8">
              {[
                { number: '20', label: 'Bookings', status: 'Excel works', color: 'text-green-600' },
                { number: '50', label: 'Bookings', status: 'You\'re stressed', color: 'text-yellow-600' },
                { number: '100+', label: 'Bookings', status: 'You\'re drowning', color: 'text-red-600' },
              ].map((stage, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, y: 30 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.2 }}
                >
                  <Card className="hover-lift border-2">
                    <CardContent className="p-8 text-center">
                      <div className={`text-6xl font-bold mb-2 ${stage.color}`}>{stage.number}</div>
                      <div className="text-lg font-semibold text-gray-700 mb-2">{stage.label}</div>
                      <div className={`text-sm font-medium ${stage.color}`}>{stage.status}</div>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* What If Operations Briefed You */}
        <section className="py-24 bg-transparent">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                What If Operations <span className="text-gradient-violet">Briefed You?</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                Every morning at 8:30 AM, know exactly what needs your attention.
              </p>
            </motion.div>

            <motion.div
              initial={{ opacity: 0, scale: 0.95 }}
              whileInView={{ opacity: 1, scale: 1 }}
              viewport={{ once: true }}
              className="max-w-2xl mx-auto"
            >
              <Card className="bg-white shadow-premium-xl border-0">
                <CardContent className="p-8">
                  <div className="flex items-center justify-between mb-6">
                    <div className="flex items-center space-x-3">
                      <div className="w-12 h-12 rounded-full bg-gradient-to-br from-indigo-600 to-violet-600 flex items-center justify-center">
                        <MessageSquare className="w-6 h-6 text-white" />
                      </div>
                      <div>
                        <div className="font-semibold text-gray-900">Daily Briefing</div>
                        <div className="text-sm text-gray-500">8:30 AM</div>
                      </div>
                    </div>
                    <Badge className="bg-green-100 text-green-700">Live</Badge>
                  </div>

                  <div className="space-y-4">
                    {briefingItems.map((item, index) => (
                      <motion.div
                        key={index}
                        initial={{ opacity: 0, x: -20 }}
                        whileInView={{ opacity: 1, x: 0 }}
                        viewport={{ once: true }}
                        transition={{ delay: index * 0.1 }}
                        className={`flex items-center space-x-3 p-4 rounded-lg ${item.bg}`}
                      >
                        <item.icon className={`w-5 h-5 ${item.color}`} />
                        <span className="font-medium text-gray-900">{item.text}</span>
                      </motion.div>
                    ))}
                  </div>
                </CardContent>
              </Card>
            </motion.div>
          </div>
        </section>

        {/* How It Works - Core Pillars */}
        <section className="py-24 bg-white rounded-[3rem] mx-4 shadow-sm border border-gray-100">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                How It <span className="text-gradient-indigo">Works</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                Six core pillars that transform operational chaos into controlled execution.
              </p>
            </motion.div>

            <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
              {corePillars.map((pillar, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, y: 30 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.1 }}
                >
                  <Card className="h-full hover-lift border-2 hover:border-indigo-200 transition-all">
                    <CardContent className="p-8">
                      <div className={`w-14 h-14 rounded-xl bg-gradient-to-br ${pillar.color} flex items-center justify-center mb-6 shadow-lg`}>
                        <pillar.icon className="w-7 h-7 text-white" />
                      </div>
                      <h3 className="text-xl font-bold text-gray-900 mb-3">{pillar.title}</h3>
                      <p className="text-gray-600 leading-relaxed">{pillar.description}</p>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* AI Agents Overview */}
        <section className="py-24 bg-gradient-to-br from-indigo-900 via-violet-900 to-blue-900 text-white relative overflow-hidden rounded-[3rem] mx-4 mt-12 shadow-2xl">
          <div className="absolute inset-0 grid-bg-dense opacity-20"></div>
          
          <div className="relative container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <Badge className="mb-6 bg-white/10 text-white border-white/20 backdrop-blur-sm px-4 py-2">
                <Brain className="w-4 h-4 mr-2" />
                AI-Powered
              </Badge>
              <h2 className="text-4xl md:text-5xl font-bold mb-6">
                24/7 Sales & Support <span className="text-emerald-400">Without Hiring</span>
              </h2>
              <p className="text-xl text-gray-200 leading-relaxed mb-8">
                AI agents that handle inquiries, qualify leads, and support clients around the clock.
              </p>
              <Link to="/ai-agents">
                <Button size="lg" className="bg-white text-indigo-600 hover:bg-gray-100 px-8 py-6 text-lg font-semibold rounded-xl">
                  Explore AI Agents
                  <ArrowRight className="ml-2 w-5 h-5" />
                </Button>
              </Link>
            </motion.div>
          </div>
        </section>

        {/* Evolution Structure */}
        <section className="py-24 bg-transparent">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Evolution <span className="text-gradient-violet">Structure</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                From visibility to intelligence, Avyron grows with your business.
              </p>
            </motion.div>

            <div className="grid md:grid-cols-3 gap-8">
              {evolutionStages.map((stage, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, y: 30 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.2 }}
                >
                  <Card className="h-full hover-lift border-2 bg-white">
                    <CardContent className="p-8">
                      <div className="text-5xl font-bold text-indigo-600 mb-4">{stage.stage}</div>
                      <h3 className="text-2xl font-bold text-gray-900 mb-3">{stage.title}</h3>
                      <p className="text-gray-600 mb-6 leading-relaxed">{stage.description}</p>
                      <ul className="space-y-2">
                        {stage.features.map((feature, idx) => (
                          <li key={idx} className="flex items-center text-sm text-gray-700">
                            <CheckCircle2 className="w-4 h-4 text-green-500 mr-2 flex-shrink-0" />
                            {feature}
                          </li>
                        ))}
                      </ul>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* Who It's For */}
        <section className="py-24 bg-white rounded-[3rem] mx-4 shadow-sm border border-gray-100">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Who It's <span className="text-gradient-indigo">For</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                Built for tour operators in the growth zone.
              </p>
            </motion.div>

            <div className="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
              <motion.div
                initial={{ opacity: 0, x: -30 }}
                whileInView={{ opacity: 1, x: 0 }}
                viewport={{ once: true }}
              >
                <Card className="bg-gray-50 border-0 h-full">
                  <CardContent className="p-8">
                    <Target className="w-12 h-12 text-indigo-600 mb-4" />
                    <h3 className="text-2xl font-bold text-gray-900 mb-4">Booking Volume</h3>
                    <p className="text-4xl font-bold text-indigo-600 mb-2">20-300</p>
                    <p className="text-gray-600">bookings per month</p>
                  </CardContent>
                </Card>
              </motion.div>

              <motion.div
                initial={{ opacity: 0, x: 30 }}
                whileInView={{ opacity: 1, x: 0 }}
                viewport={{ once: true }}
              >
                <Card className="bg-gray-50 border-0 h-full">
                  <CardContent className="p-8">
                    <Users className="w-12 h-12 text-violet-600 mb-4" />
                    <h3 className="text-2xl font-bold text-gray-900 mb-4">Team Size</h3>
                    <p className="text-4xl font-bold text-violet-600 mb-2">3-25</p>
                    <p className="text-gray-600">team members</p>
                  </CardContent>
                </Card>
              </motion.div>
            </div>
          </div>
        </section>

        {/* Trust Section */}
        <section className="py-24 bg-transparent">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Built on <span className="text-gradient-indigo">Trust</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                Enterprise-grade security and reliability for your business.
              </p>
            </motion.div>

            <div className="grid md:grid-cols-4 gap-6">
              {[
                { icon: Shield, title: 'SOC 2 Compliant', desc: 'Enterprise security' },
                { icon: Lock, title: 'End-to-End Encryption', desc: 'Your data is safe' },
                { icon: Zap, title: '99.9% Uptime', desc: 'Always available' },
                { icon: CheckCircle2, title: 'GDPR Ready', desc: 'Privacy first' },
              ].map((item, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.1 }}
                >
                  <Card className="text-center hover-lift bg-white">
                    <CardContent className="p-6">
                      <item.icon className="w-10 h-10 text-indigo-600 mx-auto mb-4" />
                      <h3 className="font-bold text-gray-900 mb-2">{item.title}</h3>
                      <p className="text-sm text-gray-600">{item.desc}</p>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* Founder Close */}
        <section className="py-24 bg-gradient-to-br from-indigo-900 via-violet-900 to-blue-900 text-white relative overflow-hidden rounded-[3rem] mx-4 shadow-2xl">
          <div className="absolute inset-0 grid-bg-dense opacity-20"></div>
          
          <div className="relative container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center"
            >
              <h2 className="text-4xl md:text-5xl font-bold mb-6">
                You Built a Business.<br />
                <span className="text-emerald-400">We Built the Infrastructure.</span>
              </h2>
              <p className="text-xl text-gray-200 leading-relaxed mb-12">
                Stop managing chaos. Start controlling operations. Avyron is the command system 
                that scales with your ambition.
              </p>
            </motion.div>
          </div>
        </section>

        {/* Final CTA */}
        <section className="py-24 bg-transparent">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Ready to Take <span className="text-gradient-indigo">Control?</span>
              </h2>
              <p className="text-xl text-gray-600 mb-12 leading-relaxed">
                See how Avyron transforms operational chaos into controlled execution.
              </p>

              <div className="flex flex-col sm:flex-row gap-4 justify-center items-center">
                <Link to="/book-demo">
                  <Button size="lg" className="bg-gradient-to-r from-indigo-600 via-violet-600 to-blue-500 hover:from-indigo-700 hover:via-violet-700 hover:to-blue-600 text-white px-8 py-6 text-lg font-semibold shadow-xl hover:scale-105 transition-transform rounded-xl border-0">
                    Book Your Private Demo
                    <Rocket className="ml-2 w-5 h-5" />
                  </Button>
                </Link>
                <Link to="/product">
                  <Button size="lg" variant="outline" className="border-2 border-indigo-600 text-indigo-600 hover:bg-indigo-50 px-8 py-6 text-lg font-semibold rounded-xl">
                    Explore the Platform
                  </Button>
                </Link>
              </div>
            </motion.div>
          </div>
        </section>
      </div>
    </>
  );
};

export default HomePage;