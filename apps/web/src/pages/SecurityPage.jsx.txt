import React from 'react';
import { Helmet } from 'react-helmet';
import { motion } from 'framer-motion';
import { Card, CardContent } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { Shield, Lock, Server, Key, CheckCircle2, Database, Activity } from 'lucide-react';

const SecurityPage = () => {
  const securityFeatures = [
    {
      icon: Lock,
      title: 'End-to-End Encryption',
      description: 'All data is encrypted in transit using TLS 1.3 and at rest using AES-256 encryption.'
    },
    {
      icon: Server,
      title: 'Infrastructure Security',
      description: 'Hosted on enterprise-grade cloud infrastructure with strict physical and network security.'
    },
    {
      icon: Key,
      title: 'Access Control',
      description: 'Role-based access control (RBAC), MFA support, and detailed audit logging.'
    },
    {
      icon: Database,
      title: 'Data Backup',
      description: 'Automated daily backups with point-in-time recovery and geo-redundant storage.'
    },
  ];

  const certifications = [
    { name: 'SOC 2 Type II', status: 'Compliant', icon: Shield },
    { name: 'GDPR', status: 'Ready', icon: CheckCircle2 },
    { name: 'ISO 27001', status: 'Aligned', icon: Activity },
  ];

  return (
    <>
      <Helmet>
        <title>Security - Avyron</title>
        <meta name="description" content="Enterprise-grade security for your operational data. Learn about Avyron's security practices and certifications." />
      </Helmet>

      <div className="min-h-screen pt-20">
        {/* Hero Section */}
        <section className="py-24 bg-gradient-to-br from-gray-900 via-indigo-900 to-violet-900 text-white relative overflow-hidden">
          <div className="absolute inset-0 grid-bg-dense opacity-20"></div>
          
          <div className="relative container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8 }}
              className="max-w-4xl mx-auto text-center"
            >
              <Badge className="mb-6 bg-white/10 text-white border-white/20 backdrop-blur-sm px-4 py-2">
                <Shield className="w-4 h-4 mr-2" />
                Enterprise Security
              </Badge>
              
              <h1 className="text-5xl md:text-6xl font-bold mb-6 leading-tight">
                Your Data is <span className="text-emerald-400">Secure</span>
              </h1>
              
              <p className="text-xl text-gray-200 mb-12 leading-relaxed">
                We treat your operational data with the highest level of security. 
                Enterprise-grade protection built into every layer of the platform.
              </p>
            </motion.div>
          </div>
        </section>

        {/* Certifications */}
        <section className="py-16 bg-white border-b border-gray-200">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <div className="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto">
              {certifications.map((cert, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.1 }}
                >
                  <Card className="text-center border-2 border-indigo-100">
                    <CardContent className="p-6">
                      <cert.icon className="w-10 h-10 text-indigo-600 mx-auto mb-4" />
                      <h3 className="font-bold text-gray-900 text-lg mb-1">{cert.name}</h3>
                      <Badge className="bg-green-100 text-green-700">{cert.status}</Badge>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* Security Features */}
        <section className="py-24 bg-gradient-to-br from-gray-50 to-indigo-50">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl font-bold text-gray-900 mb-6">
                Security <span className="text-gradient-indigo">Architecture</span>
              </h2>
            </motion.div>

            <div className="grid md:grid-cols-2 gap-8 max-w-5xl mx-auto">
              {securityFeatures.map((feature, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, x: index % 2 === 0 ? -20 : 20 }}
                  whileInView={{ opacity: 1, x: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.1 }}
                >
                  <Card className="h-full glass-card hover-lift">
                    <CardContent className="p-8">
                      <div className="flex items-start space-x-4">
                        <div className="w-12 h-12 rounded-lg bg-gradient-to-br from-indigo-500 to-violet-500 flex items-center justify-center flex-shrink-0">
                          <feature.icon className="w-6 h-6 text-white" />
                        </div>
                        <div>
                          <h3 className="text-xl font-bold text-gray-900 mb-2">{feature.title}</h3>
                          <p className="text-gray-600 leading-relaxed">{feature.description}</p>
                        </div>
                      </div>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* Penetration Testing & Vulnerability */}
        <section className="py-24 bg-white">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <div className="max-w-4xl mx-auto">
              <Card className="border-2 border-indigo-200">
                <CardContent className="p-8 md:p-12">
                  <h2 className="text-3xl font-bold text-gray-900 mb-6">Continuous Monitoring</h2>
                  <div className="space-y-6 text-gray-600">
                    <p className="text-lg">
                      Security is not a one-time setup. We continuously monitor our infrastructure and applications 
                      for potential vulnerabilities.
                    </p>
                    <ul className="space-y-4">
                      <li className="flex items-start space-x-3">
                        <CheckCircle2 className="w-6 h-6 text-green-500 flex-shrink-0" />
                        <span><strong>Annual Penetration Testing:</strong> Conducted by independent third-party security firms.</span>
                      </li>
                      <li className="flex items-start space-x-3">
                        <CheckCircle2 className="w-6 h-6 text-green-500 flex-shrink-0" />
                        <span><strong>Automated Vulnerability Scanning:</strong> Daily scans of our codebase and infrastructure.</span>
                      </li>
                      <li className="flex items-start space-x-3">
                        <CheckCircle2 className="w-6 h-6 text-green-500 flex-shrink-0" />
                        <span><strong>Bug Bounty Program:</strong> We work with security researchers to identify and fix potential issues.</span>
                      </li>
                    </ul>
                  </div>
                </CardContent>
              </Card>
            </div>
          </div>
        </section>
      </div>
    </>
  );
};

export default SecurityPage;