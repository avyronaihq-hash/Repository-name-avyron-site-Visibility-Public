import React, { useState } from 'react';
import { Helmet } from 'react-helmet';
import { Link } from 'react-router-dom';
import { motion } from 'framer-motion';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { Card, CardContent } from '@/components/ui/card';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
import { 
  AlertTriangle,
  DollarSign,
  Package,
  Users,
  Calendar,
  MessageSquare,
  BarChart3,
  CheckCircle2,
  XCircle,
  Clock,
  Shield,
  Zap,
  Brain,
  ArrowRight,
  Activity,
  FileText,
  Lock
} from 'lucide-react';

const ProductPage = () => {
  const [selectedStage, setSelectedStage] = useState('inquiry');
  const [selectedView, setSelectedView] = useState('crm');

  const lifecycleStages = [
    { id: 'inquiry', label: 'Inquiry', color: 'bg-blue-500' },
    { id: 'quote', label: 'Quote', color: 'bg-indigo-500' },
    { id: 'advance', label: 'Advance', color: 'bg-violet-500' },
    { id: 'vendor', label: 'Vendor Confirmation', color: 'bg-purple-500' },
    { id: 'final', label: 'Final Payment', color: 'bg-pink-500' },
    { id: 'travel', label: 'Travel', color: 'bg-emerald-500' },
    { id: 'closed', label: 'Closed', color: 'bg-green-500' },
  ];

  const instantFeatures = [
    { icon: AlertTriangle, title: 'Risk Detection', desc: 'Spot problems before they escalate', color: 'text-red-500' },
    { icon: DollarSign, title: 'Payment Intelligence', desc: 'Visual payment timelines & alerts', color: 'text-green-500' },
    { icon: Package, title: 'Vendor Tracking', desc: 'Reliability scores & performance', color: 'text-blue-500' },
    { icon: BarChart3, title: 'Team Load Heatmap', desc: 'Balance workload intelligently', color: 'text-purple-500' },
    { icon: Calendar, title: 'Travel Deadline Alerts', desc: 'Never miss critical dates', color: 'text-orange-500' },
    { icon: MessageSquare, title: 'Founder WhatsApp Feed', desc: 'Daily operational briefings', color: 'text-indigo-500' },
    { icon: Shield, title: 'Client Portal', desc: 'Self-service for customers', color: 'text-cyan-500' },
    { icon: Brain, title: 'AI Memory', desc: 'Learns your business patterns', color: 'text-violet-500' },
  ];

  const vendorCards = [
    { name: 'Mountain Resorts Ltd', reliability: 94, bookings: 47, avgResponse: '2.3h', status: 'Excellent' },
    { name: 'Coastal Adventures', reliability: 78, bookings: 23, avgResponse: '8.1h', status: 'Good' },
    { name: 'Desert Safaris Co', reliability: 62, bookings: 15, avgResponse: '14.5h', status: 'Needs Attention' },
  ];

  const teamMembers = [
    { name: 'Priya', bookings: 12, status: 'Overloaded', color: 'bg-red-500' },
    { name: 'Rahul', bookings: 8, status: 'Optimal', color: 'bg-green-500' },
    { name: 'Anjali', bookings: 6, status: 'Available', color: 'bg-blue-500' },
    { name: 'Vikram', bookings: 9, status: 'Optimal', color: 'bg-green-500' },
  ];

  return (
    <>
      <Helmet>
        <title>Product - Avyron Operational Control Platform</title>
        <meta name="description" content="Explore Avyron's AI-powered operational control infrastructure. Command board, payment intelligence, vendor memory, and team automation." />
      </Helmet>

      <div className="min-h-screen pt-20">
        {/* Hero Section */}
        <section className="relative py-24 bg-gradient-to-br from-gray-900 via-indigo-900 to-violet-900 text-white overflow-hidden">
          <div className="absolute inset-0 grid-bg-dense opacity-20"></div>
          
          <div className="relative container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8 }}
              className="max-w-4xl mx-auto text-center"
            >
              <Badge className="mb-6 bg-white/10 text-white border-white/20 backdrop-blur-sm px-4 py-2">
                <Activity className="w-4 h-4 mr-2" />
                Operational Control Platform
              </Badge>
              
              <h1 className="text-5xl md:text-6xl font-bold mb-6 leading-tight">
                Serious Tour Operators<br />
                <span className="text-emerald-400">Don't Run on Excel</span>
              </h1>
              
              <p className="text-xl text-gray-200 mb-12 leading-relaxed">
                AI-powered control system that transforms operational chaos into structured execution. 
                See everything. Control everything. Scale confidently.
              </p>

              <Link to="/book-demo">
                <Button size="lg" className="bg-white text-indigo-600 hover:bg-gray-100 px-8 py-6 text-lg font-semibold shadow-xl hover:scale-105 transition-transform">
                  Request a Private Walkthrough
                  <ArrowRight className="ml-2 w-5 h-5" />
                </Button>
              </Link>
            </motion.div>
          </div>
        </section>

        {/* Instant Feature Impact */}
        <section className="py-24 bg-white">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Instant Feature <span className="text-gradient-indigo">Impact</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                Every feature solves a real operational pain point.
              </p>
            </motion.div>

            <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
              {instantFeatures.map((feature, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.1 }}
                  whileHover={{ scale: 1.05, y: -5 }}
                >
                  <Card className="h-full hover-lift border-2 hover:border-indigo-200">
                    <CardContent className="p-6 text-center">
                      <feature.icon className={`w-12 h-12 mx-auto mb-4 ${feature.color}`} />
                      <h3 className="font-bold text-gray-900 mb-2">{feature.title}</h3>
                      <p className="text-sm text-gray-600">{feature.desc}</p>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* The Real Problem */}
        <section className="py-24 bg-gradient-to-br from-gray-50 to-indigo-50">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                The Real <span className="text-gradient-violet">Problem</span>
              </h2>
            </motion.div>

            <div className="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto">
              {[
                { metric: 'Numbers Increase', value: '↑ 300%', desc: 'More bookings, more complexity' },
                { metric: 'Visibility Shrinks', value: '↓ 80%', desc: 'Lost in spreadsheets' },
                { metric: 'Stress Grows', value: '↑ 500%', desc: 'Constant firefighting' },
              ].map((item, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, scale: 0.8 }}
                  whileInView={{ opacity: 1, scale: 1 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.2 }}
                >
                  <Card className="glass-card text-center">
                    <CardContent className="p-8">
                      <div className="text-sm font-semibold text-gray-600 mb-2">{item.metric}</div>
                      <div className="text-5xl font-bold text-indigo-600 mb-2">{item.value}</div>
                      <div className="text-sm text-gray-600">{item.desc}</div>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* What Avyron Actually Is */}
        <section className="py-24 bg-white">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                What Avyron <span className="text-gradient-indigo">Actually Is</span>
              </h2>
            </motion.div>

            <Tabs value={selectedView} onValueChange={setSelectedView} className="max-w-4xl mx-auto">
              <TabsList className="grid w-full grid-cols-2 mb-8">
                <TabsTrigger value="crm">Traditional CRM</TabsTrigger>
                <TabsTrigger value="avyron">Avyron</TabsTrigger>
              </TabsList>
              
              <TabsContent value="crm">
                <Card>
                  <CardContent className="p-8">
                    <h3 className="text-2xl font-bold text-gray-900 mb-6">Traditional CRM Approach</h3>
                    <div className="space-y-4">
                      {[
                        'Stores contact information',
                        'Tracks sales pipeline',
                        'Manages customer relationships',
                        'You still manage operations manually',
                        'No payment intelligence',
                        'No vendor tracking',
                        'No team load balancing',
                      ].map((item, index) => (
                        <div key={index} className="flex items-center space-x-3">
                          <XCircle className="w-5 h-5 text-red-500 flex-shrink-0" />
                          <span className="text-gray-700">{item}</span>
                        </div>
                      ))}
                    </div>
                  </CardContent>
                </Card>
              </TabsContent>
              
              <TabsContent value="avyron">
                <Card className="border-2 border-indigo-200">
                  <CardContent className="p-8">
                    <h3 className="text-2xl font-bold text-gray-900 mb-6">Avyron Control System</h3>
                    <div className="space-y-4">
                      {[
                        'Enforces booking lifecycle stages',
                        'Automates operational workflows',
                        'Provides payment intelligence & alerts',
                        'Tracks vendor reliability & performance',
                        'Balances team workload automatically',
                        'AI agents handle sales & support 24/7',
                        'Daily founder briefings via WhatsApp',
                      ].map((item, index) => (
                        <div key={index} className="flex items-center space-x-3">
                          <CheckCircle2 className="w-5 h-5 text-green-500 flex-shrink-0" />
                          <span className="text-gray-700">{item}</span>
                        </div>
                      ))}
                    </div>
                  </CardContent>
                </Card>
              </TabsContent>
            </Tabs>
          </div>
        </section>

        {/* The Command Board */}
        <section className="py-24 bg-gradient-to-br from-gray-50 to-indigo-50">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                The Command <span className="text-gradient-violet">Board</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                Every booking follows a structured lifecycle. No chaos, no missed steps.
              </p>
            </motion.div>

            <div className="max-w-6xl mx-auto">
              <div className="flex flex-wrap justify-center gap-3 mb-8">
                {lifecycleStages.map((stage) => (
                  <motion.button
                    key={stage.id}
                    onClick={() => setSelectedStage(stage.id)}
                    whileHover={{ scale: 1.05 }}
                    whileTap={{ scale: 0.95 }}
                    className={`px-6 py-3 rounded-lg font-semibold text-white transition-all ${
                      selectedStage === stage.id ? stage.color : 'bg-gray-400'
                    }`}
                  >
                    {stage.label}
                  </motion.button>
                ))}
              </div>

              <Card className="glass-card">
                <CardContent className="p-8">
                  <div className="text-center">
                    <h3 className="text-2xl font-bold text-gray-900 mb-4">
                      {lifecycleStages.find(s => s.id === selectedStage)?.label} Stage
                    </h3>
                    <p className="text-gray-600 mb-6">
                      Automated workflows, reminders, and checks ensure nothing falls through the cracks.
                    </p>
                    <div className="grid md:grid-cols-3 gap-4">
                      <div className="p-4 bg-indigo-50 rounded-lg">
                        <Clock className="w-8 h-8 text-indigo-600 mx-auto mb-2" />
                        <div className="text-sm font-semibold text-gray-900">Auto Reminders</div>
                      </div>
                      <div className="p-4 bg-violet-50 rounded-lg">
                        <CheckCircle2 className="w-8 h-8 text-violet-600 mx-auto mb-2" />
                        <div className="text-sm font-semibold text-gray-900">Status Tracking</div>
                      </div>
                      <div className="p-4 bg-blue-50 rounded-lg">
                        <AlertTriangle className="w-8 h-8 text-blue-600 mx-auto mb-2" />
                        <div className="text-sm font-semibold text-gray-900">Risk Alerts</div>
                      </div>
                    </div>
                  </div>
                </CardContent>
              </Card>
            </div>
          </div>
        </section>

        {/* Payment Intelligence */}
        <section className="py-24 bg-white">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Payment <span className="text-gradient-emerald">Intelligence</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                Visual payment timelines with color-coded status indicators.
              </p>
            </motion.div>

            <div className="max-w-4xl mx-auto">
              <Card className="glass-card">
                <CardContent className="p-8">
                  <div className="space-y-4">
                    {[
                      { label: 'Advance Payment', amount: '₹50,000', status: 'Paid', color: 'bg-green-500', date: '15 Jan' },
                      { label: 'Vendor Payment', amount: '₹30,000', status: 'Due Today', color: 'bg-yellow-500', date: '28 Jan' },
                      { label: 'Final Payment', amount: '₹70,000', status: 'Overdue', color: 'bg-red-500', date: '25 Jan' },
                    ].map((payment, index) => (
                      <motion.div
                        key={index}
                        initial={{ opacity: 0, x: -20 }}
                        whileInView={{ opacity: 1, x: 0 }}
                        viewport={{ once: true }}
                        transition={{ delay: index * 0.1 }}
                        className="flex items-center justify-between p-4 bg-gray-50 rounded-lg"
                      >
                        <div className="flex items-center space-x-4">
                          <div className={`w-3 h-3 rounded-full ${payment.color}`}></div>
                          <div>
                            <div className="font-semibold text-gray-900">{payment.label}</div>
                            <div className="text-sm text-gray-500">{payment.date}</div>
                          </div>
                        </div>
                        <div className="text-right">
                          <div className="font-bold text-gray-900">{payment.amount}</div>
                          <div className="text-sm text-gray-600">{payment.status}</div>
                        </div>
                      </motion.div>
                    ))}
                  </div>
                </CardContent>
              </Card>
            </div>
          </div>
        </section>

        {/* Vendor Reliability Memory */}
        <section className="py-24 bg-gradient-to-br from-gray-50 to-indigo-50">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Vendor Reliability <span className="text-gradient-indigo">Memory</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                Track vendor performance, response times, and reliability scores.
              </p>
            </motion.div>

            <div className="grid md:grid-cols-3 gap-6 max-w-6xl mx-auto">
              {vendorCards.map((vendor, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, rotateY: 90 }}
                  whileInView={{ opacity: 1, rotateY: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.2 }}
                  whileHover={{ scale: 1.05, rotateY: 5 }}
                >
                  <Card className="hover-lift">
                    <CardContent className="p-6">
                      <div className="text-center mb-4">
                        <div className="text-4xl font-bold text-indigo-600 mb-1">{vendor.reliability}%</div>
                        <div className="text-sm text-gray-600">{vendor.status}</div>
                      </div>
                      <h3 className="font-bold text-gray-900 mb-4">{vendor.name}</h3>
                      <div className="space-y-2 text-sm">
                        <div className="flex justify-between">
                          <span className="text-gray-600">Bookings:</span>
                          <span className="font-semibold">{vendor.bookings}</span>
                        </div>
                        <div className="flex justify-between">
                          <span className="text-gray-600">Avg Response:</span>
                          <span className="font-semibold">{vendor.avgResponse}</span>
                        </div>
                      </div>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* Team Automation & Workload Control */}
        <section className="py-24 bg-white">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Team Automation & <span className="text-gradient-violet">Workload Control</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                See who's overloaded, who's available, and distribute work intelligently.
              </p>
            </motion.div>

            <div className="grid md:grid-cols-4 gap-6 max-w-4xl mx-auto">
              {teamMembers.map((member, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.1 }}
                  whileHover={{ scale: 1.05 }}
                >
                  <Card className="hover-lift">
                    <CardContent className="p-6 text-center">
                      <div className={`w-16 h-16 rounded-full ${member.color} mx-auto mb-4 flex items-center justify-center text-white font-bold text-2xl`}>
                        {member.name[0]}
                      </div>
                      <h3 className="font-bold text-gray-900 mb-2">{member.name}</h3>
                      <div className="text-2xl font-bold text-indigo-600 mb-1">{member.bookings}</div>
                      <div className="text-sm text-gray-600">{member.status}</div>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* Real-Time Risk Detection */}
        <section className="py-24 bg-gradient-to-br from-red-50 to-orange-50">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Real-Time <span className="text-red-600">Risk Detection</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                Spot problems before they escalate with intelligent risk monitoring.
              </p>
            </motion.div>

            <div className="grid md:grid-cols-3 gap-6 max-w-5xl mx-auto">
              {[
                { level: 'High Risk', count: 3, color: 'bg-red-500', desc: 'Immediate attention needed' },
                { level: 'Medium Risk', count: 7, color: 'bg-yellow-500', desc: 'Monitor closely' },
                { level: 'Low Risk', count: 15, color: 'bg-green-500', desc: 'On track' },
              ].map((risk, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, scale: 0.8 }}
                  whileInView={{ opacity: 1, scale: 1 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.2 }}
                >
                  <Card className="glass-card text-center">
                    <CardContent className="p-8">
                      <div className={`w-20 h-20 rounded-full ${risk.color} mx-auto mb-4 flex items-center justify-center text-white font-bold text-3xl`}>
                        {risk.count}
                      </div>
                      <h3 className="font-bold text-gray-900 mb-2">{risk.level}</h3>
                      <p className="text-sm text-gray-600">{risk.desc}</p>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* Daily Founder Control Feed */}
        <section className="py-24 bg-white">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Daily Founder <span className="text-gradient-indigo">Control Feed</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                WhatsApp briefings every morning with what needs your attention.
              </p>
            </motion.div>

            <div className="max-w-md mx-auto">
              <Card className="glass-card shadow-premium-xl">
                <CardContent className="p-6">
                  <div className="bg-green-500 text-white p-4 rounded-t-lg mb-4">
                    <div className="flex items-center space-x-2">
                      <MessageSquare className="w-5 h-5" />
                      <span className="font-semibold">Avyron Daily Briefing</span>
                    </div>
                  </div>
                  <div className="space-y-3 text-sm">
                    <div className="bg-gray-100 p-3 rounded-lg">
                      <p className="text-gray-900">🌅 Good morning! Here's your operational snapshot:</p>
                    </div>
                    <div className="bg-gray-100 p-3 rounded-lg">
                      <p className="text-gray-900">⚠️ 3 bookings need immediate attention</p>
                    </div>
                    <div className="bg-gray-100 p-3 rounded-lg">
                      <p className="text-gray-900">💰 ₹2.4L in overdue payments</p>
                    </div>
                    <div className="bg-gray-100 p-3 rounded-lg">
                      <p className="text-gray-900">📦 4 vendor confirmations pending</p>
                    </div>
                    <div className="bg-gray-100 p-3 rounded-lg">
                      <p className="text-gray-900">👤 Priya is overloaded (12 bookings)</p>
                    </div>
                  </div>
                </CardContent>
              </Card>
            </div>
          </div>
        </section>

        {/* Operational Dashboard */}
        <section className="py-24 bg-gradient-to-br from-gray-50 to-indigo-50">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Operational <span className="text-gradient-violet">Dashboard</span>
              </h2>
              <p className="text-xl text-gray-600 leading-relaxed">
                Real-time visibility into every aspect of your operations.
              </p>
            </motion.div>

            <div className="grid md:grid-cols-4 gap-6 max-w-6xl mx-auto">
              {[
                { label: 'Active Bookings', value: '47', icon: FileText, color: 'text-blue-600' },
                { label: 'Pending Payments', value: '₹8.2L', icon: DollarSign, color: 'text-green-600' },
                { label: 'Team Members', value: '12', icon: Users, color: 'text-purple-600' },
                { label: 'Vendors Tracked', value: '34', icon: Package, color: 'text-orange-600' },
              ].map((stat, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.1 }}
                >
                  <Card className="hover-lift">
                    <CardContent className="p-6 text-center">
                      <stat.icon className={`w-10 h-10 mx-auto mb-4 ${stat.color}`} />
                      <div className="text-3xl font-bold text-gray-900 mb-2">{stat.value}</div>
                      <div className="text-sm text-gray-600">{stat.label}</div>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* AI Intelligence Evolution */}
        <section className="py-24 bg-gradient-to-br from-indigo-900 via-violet-900 to-blue-900 text-white relative overflow-hidden">
          <div className="absolute inset-0 grid-bg-dense opacity-20"></div>
          
          <div className="relative container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <Badge className="mb-6 bg-white/10 text-white border-white/20 backdrop-blur-sm px-4 py-2">
                <Brain className="w-4 h-4 mr-2" />
                AI-Powered Intelligence
              </Badge>
              <h2 className="text-4xl md:text-5xl font-bold mb-6">
                AI Intelligence <span className="text-emerald-400">Evolution</span>
              </h2>
              <p className="text-xl text-gray-200 leading-relaxed">
                The system learns your business patterns and provides predictive insights.
              </p>
            </motion.div>

            <div className="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto">
              {[
                { title: 'Pattern Recognition', desc: 'Identifies booking trends and seasonal patterns' },
                { title: 'Predictive Alerts', desc: 'Warns about potential issues before they happen' },
                { title: 'Smart Recommendations', desc: 'Suggests optimal vendor assignments and pricing' },
              ].map((item, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.2 }}
                >
                  <Card className="bg-white/10 backdrop-blur-sm border-white/20 text-white">
                    <CardContent className="p-6 text-center">
                      <Brain className="w-12 h-12 mx-auto mb-4 text-emerald-400" />
                      <h3 className="font-bold text-xl mb-3">{item.title}</h3>
                      <p className="text-gray-200">{item.desc}</p>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* Technology & Trust */}
        <section className="py-24 bg-white">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Technology & <span className="text-gradient-indigo">Trust</span>
              </h2>
            </motion.div>

            <div className="grid md:grid-cols-4 gap-6">
              {[
                { icon: Shield, title: 'SOC 2 Compliant', desc: 'Enterprise security standards' },
                { icon: Lock, title: 'End-to-End Encryption', desc: 'Your data is protected' },
                { icon: Zap, title: '99.9% Uptime', desc: 'Always available when you need it' },
                { icon: CheckCircle2, title: 'GDPR Ready', desc: 'Privacy-first architecture' },
              ].map((item, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, y: 20 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  viewport={{ once: true }}
                  transition={{ delay: index * 0.1 }}
                >
                  <Card className="text-center hover-lift">
                    <CardContent className="p-6">
                      <item.icon className="w-10 h-10 text-indigo-600 mx-auto mb-4" />
                      <h3 className="font-bold text-gray-900 mb-2">{item.title}</h3>
                      <p className="text-sm text-gray-600">{item.desc}</p>
                    </CardContent>
                  </Card>
                </motion.div>
              ))}
            </div>
          </div>
        </section>

        {/* Who This Is For */}
        <section className="py-24 bg-gradient-to-br from-gray-50 to-indigo-50">
          <div className="container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center mb-16"
            >
              <h2 className="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Who This Is <span className="text-gradient-violet">For</span>
              </h2>
            </motion.div>

            <div className="max-w-3xl mx-auto">
              <Card className="glass-card">
                <CardContent className="p-8">
                  <div className="space-y-4">
                    {[
                      'Tour operators handling 20-300 bookings/month',
                      'Teams of 3-25 people feeling operational strain',
                      'Businesses outgrowing spreadsheets and manual processes',
                      'Founders who want control without micromanagement',
                      'Operations teams drowning in coordination tasks',
                      'Companies ready to scale systematically',
                    ].map((item, index) => (
                      <motion.div
                        key={index}
                        initial={{ opacity: 0, x: -20 }}
                        whileInView={{ opacity: 1, x: 0 }}
                        viewport={{ once: true }}
                        transition={{ delay: index * 0.1 }}
                        className="flex items-center space-x-3"
                      >
                        <CheckCircle2 className="w-6 h-6 text-green-500 flex-shrink-0" />
                        <span className="text-gray-700 text-lg">{item}</span>
                      </motion.div>
                    ))}
                  </div>
                </CardContent>
              </Card>
            </div>
          </div>
        </section>

        {/* Final CTA */}
        <section className="py-24 bg-gradient-to-br from-indigo-900 via-violet-900 to-blue-900 text-white relative overflow-hidden">
          <div className="absolute inset-0 grid-bg-dense opacity-20"></div>
          
          <div className="relative container mx-auto px-4 sm:px-6 lg:px-8">
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              className="max-w-4xl mx-auto text-center"
            >
              <h2 className="text-4xl md:text-5xl font-bold mb-6">
                Ready to See It in <span className="text-emerald-400">Action?</span>
              </h2>
              <p className="text-xl text-gray-200 mb-12 leading-relaxed">
                Book a private walkthrough and see how Avyron transforms your operations.
              </p>

              <Link to="/book-demo">
                <Button size="lg" className="bg-white text-indigo-600 hover:bg-gray-100 px-8 py-6 text-lg font-semibold shadow-xl hover:scale-105 transition-transform">
                  Book a Demo
                  <ArrowRight className="ml-2 w-5 h-5" />
                </Button>
              </Link>
            </motion.div>
          </div>
        </section>
      </div>
    </>
  );
};

export default ProductPage;